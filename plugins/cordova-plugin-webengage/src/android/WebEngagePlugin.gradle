buildscript {
    repositories {
        mavenCentral()
        jcenter()
    }
}

allprojects {
    repositories {
        mavenCentral();
        jcenter();
    }
}
dependencies {
    implementation fileTree(include: ['*.aar'], dir: 'libs')
    compileOnly files("src/main/libs/android-sdk-debug.aar")

    //api 'com.webengage:android-sdk:[3.20,)'

}
